import { hero } from "./hero";
import { identity } from "./identity";
import { domains, exploration, openSource } from "./content";
import { contact } from "./contact";

const aboutData = {
  hero,
  identity,
  domains,
  exploration,
  openSource,
  contact,
};

export default aboutData;
